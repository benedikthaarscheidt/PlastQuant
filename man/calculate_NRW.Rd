% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3.R
\name{calculate_NRW}
\alias{calculate_NRW}
\title{Calculate Norm of Reaction Width (NRW)}
\usage{
calculate_NRW(
  trait_values,
  env_values = NULL,
  group_values = NULL,
  across = FALSE
)
}
\arguments{
\item{trait_values}{A numeric vector representing measured trait values.}

\item{env_values}{(Optional) A numeric vector representing environments. If NULL, assumes equidistant environments.}

\item{group_values}{(Optional) A vector indicating genotypes or other grouping factors. If provided, NRW is computed per group.}

\item{across}{Logical. If TRUE, calculates NRW **across all environments**.}
}
\value{
A numeric value or named vector representing NRW.
}
\description{
Computes the Norm of Reaction Width (NRW) for a given trait across multiple environments.
}
\details{
NRW is calculated as:
\deqn{NRW = \max(\bar{z}_e) - \min(\bar{z}_e)}
where **\(\bar{z}_e\)** is the mean trait value per environment.

If `across = TRUE`, NRW is computed **for all environments**.

If an environment has **only one measurement**, NRW is set to `NA`.
}
\examples{
trait_values = c(10, 12, 15, 17, 20, 25, 30)
env_values = c(1, 1, 2, 2, 3, 3, 3)
calculate_NRW(trait_values, env_values)

}
